# This file can be generated by copying pyGBot/hashpw.py into the directory that
# contains users.ini, and then by running it.
#
# Conversely, if you are aware of the hash of the password of the user, you can 
# insert it as such (replacing things in <> with their corresponding entries:
#
# [<Username>]
# userlevel = <Numeric level>
# passhash = <Hash>
